{
  "name": "securelife-insurance-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "db:seed": "cd server && npm run db:seed"
  }
}
